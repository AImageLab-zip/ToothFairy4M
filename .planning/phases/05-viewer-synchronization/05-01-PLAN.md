---
phase: 05-viewer-synchronization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/brain/patient_detail_content.html
  - static/js/modality_viewers/niivue_viewer.js
  - static/js/viewer_grid.js
autonomous: true
must_haves:
  truths:
    - "NiiVue library loads correctly without errors"
    - "Viewer grid displays properly with 2x2 windows"
    - "Modality loading works without errors"
    - "All viewer features (orientation controls, slice navigation) function correctly"
    - "Synchronized scrolling functionality is ready for implementation"
  artifacts:
    - path: "templates/brain/patient_detail_content.html"
      provides: "NiiVue CDN script tag with version 0.70.0"
      min_lines: 180
    - path: "static/js/modality_viewers/niivue_viewer.js"
      provides: "NiiVueViewer class with proper initialization and error handling"
      exports: ["NiiVueViewer"]
    - path: "static/js/viewer_grid.js"
      provides: "Viewer grid management with async loading and caching"
      exports: ["loadModalityInWindow", "volumeCache"]
  key_links:
    - from: "templates/brain/patient_detail_content.html"
      to: "NiiVue library"
      via: "CDN script tag line 180"
      pattern: "https://cdn.jsdelivr.net/npm/@niivue/niivue@0.70.0/dist/niivue.min.js"
    - from: "static/js/modality_viewers/niivue_viewer.js"
      to: "window.niivue.Niivue"
      via: "constructor check and usage"
      pattern: "new window.niivue.Niivue"
    - from: "static/js/viewer_grid.js"
      to: "NiiVueViewer class"
      via: "window.NiiVueViewer instantiation"
      pattern: "new window.NiiVueViewer"
---

<objective>
Verify that the NiiVue library loading fix is working correctly and all viewer functionality is operational before implementing synchronization features.

Purpose: This verification ensures that the fix for the NiiVue library version 0.66.0 compatibility issue (updated to 0.70.0) is successful and that all existing viewer functionality works as expected. This is a prerequisite for implementing synchronized scrolling in Phase 5.

Output: Confirmed working NiiVue integration with all viewer features functional.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-viewer-display/04-VERIFICATION.md
@.planning/debug/resolved/niivue-loading-issue.md
@.planning/phases/04-viewer-display/04-03-SUMMARY.md
@.planning/phases/04-viewer-display/04-02-SUMMARY.md
@.planning/phases/04-viewer-display/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify NiiVue Library Loading</name>
  <files>templates/brain/patient_detail_content.html</files>
  <action>
    Check that the NiiVue library is properly loaded from CDN with version 0.70.0. The script tag should be:
    &lt;script src="https://cdn.jsdelivr.net/npm/@niivue/niivue@0.70.0/dist/niivue.min.js"&gt;&lt;/script&gt;

    Also verify that the script is loaded before niivue_viewer.js and viewer_grid.js.
  </action>
  <verify>
    Open browser console and verify that no "NiiVue library not loaded" errors appear. Check that window.niivue is defined and contains the Niivue constructor function.
  </verify>
  <done>
    No NiiVue loading errors in browser console. window.niivue.Niivue is available as a constructor function.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test Viewer Grid Display</name>
  <files>templates/brain/patient_detail_content.html</files>
  <action>
    Verify that the 2x2 viewer grid is displayed correctly with:
    - Four empty viewer windows with proper styling
    - Drop hints for each window
    - Available modality chips for drag-and-drop
    - Help modal button
  </action>
  <verify>
    In browser, verify that:
    1. The 2x2 grid is visible with proper styling
    2. Each window has a drop hint with arrow and text
    3. Modality chips are displayed correctly
    4. Help modal button is present
  </verify>
  <done>
    Viewer grid displays properly with 4 windows, drop hints, and modality chips.
  </done>
</task>

<task type="auto">
  <name>Task 3: Confirm Modality Loading Functionality</name>
  <files>static/js/modality_viewers/niivue_viewer.js, static/js/viewer_grid.js</files>
  <action>
    Test that modality loading works by:
    1. Dragging a modality chip onto an empty window
    2. Verifying that the volume loads correctly
    3. Checking that the viewer displays the volume in the window
    4. Confirming that no errors occur during the loading process
  </action>
  <verify>
    In browser, verify that:
    1. Modality can be dragged and dropped onto a window
    2. Volume loads without errors
    3. The volume is displayed in the window canvas
    4. No console errors related to NiiVue or loading
  </verify>
  <done>
    Modalities load correctly in viewer windows without errors.
  </done>
</task>

<task type="auto">
  <name>Task 4: Validate Viewer Features</name>
  <files>static/js/modality_viewers/niivue_viewer.js, static/js/viewer_grid.js</files>
  <action>
    Test all viewer features:
    1. Orientation switching (Axial/Sagittal/Coronal buttons)
    2. Slice navigation with mouse wheel
    3. Window clearing (right-click on window)
    4. Volume replacement (dragging new modality to existing window)
    5. Error handling for invalid files
  </action>
  <verify>
    In browser, verify that:
    1. Orientation buttons switch views correctly
    2. Mouse wheel scrolling works for slice navigation
    3. Right-click clears window content
    4. Dragging new modality replaces existing one
    5. Error messages appear for invalid files
  </verify>
  <done>
    All viewer features work correctly with no functional issues.
  </done>
</task>

</tasks>

<verification>
Verify that all NiiVue integration points are working correctly:
1. CDN script tag loads version 0.70.0
2. NiiVue library is available as window.niivue.Niivue constructor
3. Viewer grid displays correctly with 4 windows
4. Modality loading works without errors
5. All viewer features function properly
</verification>

<success_criteria>
All observable truths from the goal are verified:
- NiiVue library loads correctly without errors
- Viewer grid displays properly with 2x2 windows
- Modality loading works without errors
- All viewer features (orientation controls, slice navigation) function correctly
- Synchronized scrolling functionality is ready for implementation
</success_criteria>

<output>
After completion, create `.planning/phases/05-viewer-synchronization/05-01-SUMMARY.md`
</output>