---
phase: 02-brain-upload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - maxillo/file_utils.py
  - brain/management/commands/setup_brain_modalities.py
autonomous: true

must_haves:
  truths:
    - "Brain modality uploads complete immediately without processing jobs"
    - "Brain project has T1, T2, FLAIR, T1c modalities registered and active"
  artifacts:
    - path: "maxillo/file_utils.py"
      provides: "Brain modality slugs in no_processing_modalities list"
      contains: "braintumor-mri-t1"
      min_lines: 220
    - path: "brain/management/commands/setup_brain_modalities.py"
      provides: "Management command to create brain modalities"
      exports: ["Command"]
      min_lines: 30
  key_links:
    - from: "maxillo/file_utils.py"
      to: "no_processing_modalities list"
      via: "Brain slugs added to list"
      pattern: "braintumor-mri-(t1|t2|flair|t1c)"
    - from: "Brain project"
      to: "Modality records"
      via: "ManyToMany relationship"
      pattern: "project.modalities.add"
---

<objective>
Enable brain MRI modality uploads with immediate availability (no processing queue).

Purpose: Brain modalities (.nii.gz files) must appear immediately in FileRegistry after upload, following the same pattern as panoramic/teleradiography modalities that require no async processing.

Output: Updated file_utils.py with brain modality support, management command to create brain modality records.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-brain-upload/02-CONTEXT.md
@.planning/phases/02-brain-upload/02-RESEARCH.md
@maxillo/file_utils.py
@common/models.py
</context>

<tasks>

<task type="auto">
  <name>Add brain modalities to no_processing_modalities list</name>
  <files>maxillo/file_utils.py</files>
  <action>
Update the no_processing_modalities list (currently at line ~210) to include brain modality slugs. Add these four slugs to the existing list:
- 'braintumor-mri-t1'
- 'braintumor-mri-t2'
- 'braintumor-mri-flair'
- 'braintumor-mri-t1c'

The existing list contains: ['panoramic', 'teleradiography', 'intraoral-photo', 'rawzip']

Add the brain modalities to this list so that when save_generic_modality_file() is called with these slugs, the Job is created with status='completed' immediately (no pending processing). This allows uploaded brain MRI files to appear in patient detail without waiting for async processing.

Do NOT create separate functions or custom logic. The existing code already handles immediate availability for items in this list.
  </action>
  <verify>
Run: `grep -n "no_processing_modalities" /home/llumetti/ToothFairy4M-dev/maxillo/file_utils.py -A 10`

Confirm the list now contains all four brain modality slugs.
  </verify>
  <done>
The no_processing_modalities list in maxillo/file_utils.py includes 'braintumor-mri-t1', 'braintumor-mri-t2', 'braintumor-mri-flair', 'braintumor-mri-t1c' alongside existing modalities.
  </done>
</task>

<task type="auto">
  <name>Create management command to setup brain modalities</name>
  <files>brain/management/commands/setup_brain_modalities.py</files>
  <action>
Create a Django management command that ensures Brain project exists and has the four brain modalities registered. Follow the pattern from maxillo/management/commands/create_image_modalities.py.

The command should:
1. Get or create Project with name='Brain'
2. For each brain modality slug ('braintumor-mri-t1', 'braintumor-mri-t2', 'braintumor-mri-flair', 'braintumor-mri-t1c'):
   - Get or create Modality with slug=slug, name=friendly_name (e.g., "Brain MRI T1"), is_active=True
   - Add modality to Brain project via project.modalities.add(modality)
3. Print success message for each created/verified modality

Use friendly names:
- 'braintumor-mri-t1' -> "Brain MRI T1"
- 'braintumor-mri-t2' -> "Brain MRI T2"
- 'braintumor-mri-flair' -> "Brain MRI FLAIR"
- 'braintumor-mri-t1c' -> "Brain MRI T1c"

The command should be idempotent (safe to run multiple times).

Create directory brain/management/commands/ if it doesn't exist. Also create brain/management/__init__.py as empty file.
  </action>
  <verify>
Run: `python /home/llumetti/ToothFairy4M-dev/manage.py setup_brain_modalities`

Should output messages confirming Brain project exists and four modalities are registered.

Then run Django shell query:
```python
from common.models import Project, Modality
brain = Project.objects.get(name='Brain')
print(brain.modalities.filter(is_active=True).values_list('slug', flat=True))
```

Should show all four brain modality slugs.
  </verify>
  <done>
Brain project exists with four active modalities (T1, T2, FLAIR, T1c) registered in database and linked via ManyToMany. Management command runs successfully and is idempotent.
  </done>
</task>

</tasks>

<verification>
After completing both tasks:

1. Check no_processing_modalities list:
   ```bash
   grep -A 10 "no_processing_modalities" maxillo/file_utils.py
   ```
   Should show all four brain modality slugs in the list.

2. Verify brain modalities in database:
   ```bash
   python manage.py shell -c "from common.models import Project; p = Project.objects.get(name='Brain'); print(list(p.modalities.values_list('slug', 'name')))"
   ```
   Should show four tuples with brain modality slugs and names.

3. Confirm brain templates exist:
   ```bash
   ls templates/common/upload/modalities/braintumor-mri-*.html
   ```
   Should list four template files (t1, t2, flair, t1c).
</verification>

<success_criteria>
Infrastructure ready for brain uploads:
- [ ] no_processing_modalities list contains all four brain modality slugs
- [ ] Brain project exists in database with name='Brain'
- [ ] Four Modality records exist with correct slugs and friendly names
- [ ] Brain project.modalities ManyToMany includes all four modalities
- [ ] Management command setup_brain_modalities runs without errors
- [ ] Modality templates exist for all four brain modalities (verified via ls)
</success_criteria>

<output>
After completion, create `.planning/phases/02-brain-upload/02-01-SUMMARY.md`
</output>
